import React from "react";
import { useNavigate } from "react-router-dom";

const DrawerDash = () => {
	const navigate = useNavigate();

	return <div>Sample Drawer / Sidebar</div>;
};

export default DrawerDash;
